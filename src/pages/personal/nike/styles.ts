import styled from 'styled-components'

export const GalleryProject = styled.div`
  column-count: 3;
  column-gap: 8px;

  img {
    width: 100%;
    margin-bottom: 4px;
    cursor: pointer;
    transition: filter 1s;
  }
`
